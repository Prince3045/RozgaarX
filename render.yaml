services:
  - type: web
    name: rozgaarx-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    region: singapore
    envVars:
      - key: PORT
        value: 8081
      - key: JWT_SECRET
        generateValue: true
